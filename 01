#include <stdio.h>
#include <stdlib.h>

int main() {
    int x;

    printf("정수 하나를 입력하세요: \n");
    scanf("%d", &x);

    int X = (x < 0) ? -x : x;

    printf("절대값은 %d 입니다.\n", X);

    return 0;
}
